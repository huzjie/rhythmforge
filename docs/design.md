# 核心设计决策

1. **能力与偏差分离**：mock 后端用加法分离 `truth` 与位置偏置，保证基准分数反映真实能力。
2. **零依赖内核**：边缘部署需要极小的依赖面，核心只用标准库。
3. **确定性优先**：所有 mock 信号用 md5 做 seed，跨进程、跨平台可复现（不依赖 `hash()` 的 PYTHONHASHSEED）。
4. **规划器与引擎解耦**：规划器只依赖 `DecisionEnv` 接口，与具体后端无关。
5. **生存空间作为一等公民**：把"回旋余地"量化进分支评分，这是长程生存的关键。
