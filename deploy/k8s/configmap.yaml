apiVersion: v1
kind: ConfigMap
metadata:
  name: rhythmforge-config
  namespace: rhythmforge
data:
  config.yaml: |
    backend:
      kind: mock
      skill: 0.7
      noise: 0.2
    engine:
      mode: argmax
    planner:
      kind: hybrid
